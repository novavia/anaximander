{% import 'sqlalchemy/functions.py.j2' as functions %}
from typing import Any, List, Optional

from sqlalchemy import (
    JSON,
    ForeignKey,
    Index,
    String,
    Text,
    UniqueConstraint,
)
from sqlalchemy.orm import (
    Mapped,
    mapped_column,
    relationship,
)

from . import Base


{% for modeltype in compiler.modeltypes -%}
    {{ functions.modelclass(modeltype) }}
{%- endfor %}
